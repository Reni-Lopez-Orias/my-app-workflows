# backend

API NestJS + Prisma + PostgreSQL del proyecto. Ver el [README de la raíz](../README.md) para cómo levantar todo (nativo o con Docker), variables de entorno y endpoints.

```bash
npm install
npx prisma migrate dev
npm run start:dev
```
