# frontend

React (Vite + TypeScript) del proyecto. Ver el [README de la raíz](../README.md) para cómo levantar todo (nativo o con Docker) y las variables de entorno.

```bash
npm install
npm run dev
```
